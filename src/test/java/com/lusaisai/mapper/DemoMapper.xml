<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper  namespace="com.lusaisai.dao.DemoMapper">

    <select  id="selectAll"    resultType="com.lusaisai.po.ProjInfo" >
        SELECT
            t2.id AS sectionId ,
            t2.proj_id AS projId ,
            t2.section_name AS sectionName
        FROM b_proj_base_info AS t1
        JOIN b_proj_section_info AS t2 ON t1.id=t2.proj_id
        WHERE t2.proj_id=#{projId}
        AND t2.section_name LIKE CONCAT('%',#{sectionName},'%')
    </select>


</mapper>
